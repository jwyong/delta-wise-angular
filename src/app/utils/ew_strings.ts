/**
 * string values for users
 */
export class EWStrings {
    //=== validation
    // required
    public static VAL_REQUIRED: string = `You must enter a value`;

    // email
    public static VAL_INVALID_EMAIL: string = 'Not a valid email';

    // password
    public static VAL_INVALID_PWORD: string = 'Password must have at least 8 characters';

    //=== forgotPword
    // public static F_PWORD_SUCCESS: string = 'An email has been sent to %';
}